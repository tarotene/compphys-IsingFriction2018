#include <bits/stdc++.h>

struct Model {
    ;
};

struct Job {
    ;
};

Model defineModel();

Job defineJob(Model &);

int executeJob(Job &);

int main(int argc, char const *argv[])
{
    int err = 0;

    Model model = defineModel();

    Job job = defineJob(model);

    int err = executeJob(job);

    return 0;
}

Model defineModel() {
    ;
}

Job defineJob(Model &model) {
    ;
}

int executeJob(Job &job) {
    ;
}